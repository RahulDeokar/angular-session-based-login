{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/session-based-login-fronted/browser" }
    }
  ],
  "routes": [
    { "src": "/.*", "dest": "/index.html" }
  ]
}
