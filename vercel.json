{
  "version": 2,
  "builds": [
    {
      "src": "dist/**/browser",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist/browser" }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.html"
    }
  ]
}
